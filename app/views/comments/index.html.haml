%h1 Listing comments

%table
  %tr
    %th Nickname
    %th Content
    %th Has children?
    %th Parent
    %th Cascade level
    %th
    %th
    %th

  - @comments.each do |comment|
    %tr
      %td= comment.nickname
      %td= comment.content
      %td= comment.has_children?
      %td= comment.parent
      %td= comment.cascade_level
      %td= link_to 'Show', comment
      %td= link_to 'Edit', edit_comment_path(comment)
      %td= link_to 'Destroy', comment, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Comment', new_comment_path
