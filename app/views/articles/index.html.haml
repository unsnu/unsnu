%h2= @board.name

%table.table
  %tr
    %th.hidden-xs #
    %th 제목
    %th 추천
    %th 글쓴이
    %th.hidden-xs 날짜
    %th.hidden-xs 조회수
  - @articles.each do |article|
    %tr
      %td.hidden-xs= article.id
      %td= "#{article.title} [#{article.comment_count}]"
      %td= article.upvote
      %td= article.nickname
      %td.hidden-xs= article.created_at
      %td.hidden-xs= article.view_count

.clearfix
  = link_to '글쓰기', new_board_article_path(@board.id), class: 'btn btn-primary pull-right'